<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a class="bg-red-500 mb-5 inline-block rounded-md px-3 py-2 text-sm" href="/logout">Logout</a>
        <a class="bg-blue-500 mb-5 inline-block rounded-md px-3 py-2 text-sm" href="/">Home</a>
        <h3 class="text-3xl mb-5">Hello , <%= user.name %></h3>
        <h5>You can create a Post</h5>
        <form method="post" action="/post">
            <textarea placeholder="whats in your mind" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 text-sm block rounded-md" type="submit" value="Create new post" name="" id="">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-4 p-5 border-2 border-zinc-800">
                        <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                        <p class="text-sm tracking-tight w-4/5"><%= post.content %></p>
                        <small class="mt-1 inline-block"><%= post.likes.length %> Likes</small>
                        <div class="btns mb-2 gap-4 flex mt-5">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) ? "like" : "unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">edit</a>
                        </div>
                    </div>  
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>



                  
