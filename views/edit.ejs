<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <a class="bg-red-500 mb-5 inline-block rounded-md px-3 py-2 text-sm" href="/logout">Logout</a>
        <h5>You can edit your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea placeholder="whats in your mind" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id=""><%= post.content %></textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-black text-sm block rounded-md" type="submit" value="Update post" name="" id="">
        </form>
    </div>
</body>
</html>
                  
