<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheet/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full h-full bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">read</a>
        </div>
        <h1 class="text-3xl tracking-tighter">Update User</h1>
        <form action="/update/<%= user._id %>" method="post"> 
            <input class="px-5 mt-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="name" value="<%= user.name %>" placeholder="name">
            <input class="px-5 mt-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="email" value="<%= user.email %>" placeholder="email">
            <input class="px-5 mt-5 py-3 border-2 border-zinc-800 outline-none rounded-lg bg-transparent" type="text" name="image" value="<%= user.image %>" placeholder="image url">
            <input class="px-5 mt-5 py-3 bg-yellow-500 text-black rounded-lg ml-3" type="submit" value="Update User">
        </form>
    </div>
</body>
</html>